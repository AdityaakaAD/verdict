// DEV ONLY — visual test for the JuryChamber 3D scene.
// Delete before production launch.
import { ChamberTestClient } from './client';

export default function ChamberTestPage() {
  return <ChamberTestClient />;
}
