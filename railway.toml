[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @verdict/socket build"

[deploy]
startCommand = "node apps/socket/dist/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
