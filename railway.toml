[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "pnpm --filter @verdict/socket start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
